
// const validation = (userData) => {
//     const newErrors = {}

//     if(!driver.name.trim()){
//           newErrors.name= "El nombre es obligatorio"
//     }else if(!/^[a-zA-Z\s]+$/.test(driver.name)){
//         newErrors.name = "El nombre no debe contener símbolos ni números";
  
//     }
//      // Setear los errores en el estado
//   setErrors(newErrors);

//   // Devolver verdadero si no hay errores, falso en caso contrario
//   return Object.keys(newErrors).length === 0;
// }

// export default validation;